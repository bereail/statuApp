/*import { getTitle, getYear, Statue } from "../types/statues";

export default function StatueMeta({ s }: { s: Statue }) {
return (
<div className="grid gap-1">
<h1 className="text-xl font-semibold">{getTitle(s)}</h1>
<p className="text-sm text-zinc-600">
{s.barrio ? `${s.barrio} • ` : ''}{getYear(s) ?? 's/f'}
</p>
</div>
);
}*/